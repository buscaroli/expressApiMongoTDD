{
  "name": "shiftsapi",
  "version": "1.0.0",
  "description": "NodeJS and Express API to keep track of work shifts and to be interfaced with MongoDB using Mongoose. ",
  "main": "src/index.js",
  "scripts": {
    "dev": "env-cmd -e dev nodemon src/index.js",
    "test": "env-cmd -e test jest --detectOpenHandles --forceExit",
    "test:watch": "npm run test -- --watch"
  },
  "keywords": [
    "node",
    "mongo",
    "express"
  ],
  "author": "mabus",
  "license": "ISC",
  "dependencies": {
    "env-cmd": "^10.1.0",
    "express": "^4.17.3",
    "mongoose": "^6.2.7",
    "validator": "^13.7.0"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "supertest": "^6.2.2"
  }
}
